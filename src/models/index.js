/**
 * @module Assembling_models
 * @description This module contain all models
 */
const Note = require('./note')
const User = require('./user');

const models = {
  Note,
  User
}

module.exports = models